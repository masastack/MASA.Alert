@switch (Value)
{
    case AlarmHistoryHandleStatuses.Pending:
        <MIcon Size=@Size Dark Color="remind">
            mdi-timer-outline
        </MIcon>
        break;
    case AlarmHistoryHandleStatuses.InProcess:
        <MIcon Size=@Size Dark Color="primary">
            mdi-chart-donut
        </MIcon>
        break;
    case AlarmHistoryHandleStatuses.ProcessingCompleted:
        <MIcon Size=@Size Dark Color="success">
            mdi-check
        </MIcon>
        break;
    case AlarmHistoryHandleStatuses.Notified:
        <MIcon Size=@Size Dark Color="info">
            mdi-message-text
        </MIcon>
        break;
}

@code {
    [Parameter]
    public AlarmHistoryHandleStatuses Value { get; set; }

    [Parameter]
    public StringNumber? Size { get; set; } = 16;
}
