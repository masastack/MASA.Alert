apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "masa.alert.ui.admin.fullname" . }}-test-connection"
  labels:
    {{- include "masa.alert.ui.admin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "masa.alert.ui.admin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
